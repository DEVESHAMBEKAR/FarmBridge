import 'package:flutter/material.dart';

class DeliveryRouteDetailsScreen extends StatelessWidget {
  const DeliveryRouteDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DeliveryRouteDetailsScreen')),
      body: const Center(child: Text('DeliveryRouteDetailsScreen Placeholder')),
    );
  }
}
