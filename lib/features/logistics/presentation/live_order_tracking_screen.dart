import 'package:flutter/material.dart';

class LiveOrderTrackingScreen extends StatelessWidget {
  const LiveOrderTrackingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('LiveOrderTrackingScreen')),
      body: const Center(child: Text('LiveOrderTrackingScreen Placeholder')),
    );
  }
}
