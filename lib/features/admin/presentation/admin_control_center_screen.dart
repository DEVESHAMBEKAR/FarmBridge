import 'package:flutter/material.dart';

class AdminControlCenterScreen extends StatelessWidget {
  const AdminControlCenterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AdminControlCenterScreen')),
      body: const Center(child: Text('AdminControlCenterScreen Placeholder')),
    );
  }
}
