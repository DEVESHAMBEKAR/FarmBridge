import 'package:flutter/material.dart';

class FarmDetailsVerificationScreen extends StatelessWidget {
  const FarmDetailsVerificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FarmDetailsVerificationScreen')),
      body: const Center(child: Text('FarmDetailsVerificationScreen Placeholder')),
    );
  }
}
